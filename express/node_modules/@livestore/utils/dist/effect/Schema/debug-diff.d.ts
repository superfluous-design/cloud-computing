import { Schema, SchemaAST } from 'effect';
export type DiffItem = {
    path: string;
    a: any;
    b: any;
    ast: SchemaAST.AST;
};
/**
 * Diffs two values for a given schema and traverses downwards and returns a list of differences.
 */
export declare const debugDiff: <A, I, R>(base: Schema.Schema<A, I, R>) => (a: A, b: A) => DiffItem[];
//# sourceMappingURL=debug-diff.d.ts.map