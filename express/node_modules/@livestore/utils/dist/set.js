export const difference = (a, b) => {
    const diff = new Set();
    for (const item of a) {
        if (!b.has(item)) {
            diff.add(item);
        }
    }
    return diff;
};
//# sourceMappingURL=set.js.map